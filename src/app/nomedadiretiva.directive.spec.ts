import { NomedadiretivaDirective } from './nomedadiretiva.directive';

describe('NomedadiretivaDirective', () => {
  it('should create an instance', () => {
    const directive = new NomedadiretivaDirective();
    expect(directive).toBeTruthy();
  });
});
